Make the scripts find their libraries
---
 in/scripts=wicd-client.in |    4 ++++
 in/scripts=wicd.in        |    5 ++++-
 2 files changed, 8 insertions(+), 1 deletion(-)

--- wicd.orig/in/scripts=wicd.in
+++ wicd/in/scripts=wicd.in
@@ -1,3 +1,6 @@
 #!/bin/bash
 
-exec python -O %LIB%wicd-daemon.py $@
+PYTHONPATH=%LIB%
+export PYTHONPATH
+
+exec python %LIB%wicd-daemon.py $@
--- wicd.orig/in/scripts=wicd-client.in
+++ wicd/in/scripts=wicd-client.in
@@ -2,6 +2,10 @@
 BOLD=$(tput bold)
 BLUE=$(tput setaf 4)
 NC=$(tput sgr0)
+
+PYTHONPATH=%LIB%
+export PYTHONPATH
+
 # check_firstrun()
 if [ ! -d "$HOME/.wicd" ]; then
     mkdir -p "$HOME/.wicd"
