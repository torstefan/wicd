Let the users of group "adm" read the logs
---
 wicd/wicd-daemon.py |    7 +++++--
 1 file changed, 5 insertions(+), 2 deletions(-)

--- wicd.orig/wicd/wicd-daemon.py
+++ wicd/wicd/wicd-daemon.py
@@ -1671,9 +1671,12 @@ def main(argv):
         output = ManagedStdio(logpath)
         if os.path.exists(logpath):
             try:
-                os.chmod(logpath, 0600)
+                os.chmod(logpath, 0640)
+                import grp
+                group = grp.getgrnam("adm")
+                os.chown(logpath, 0, group[2])
             except:
-                print 'unable to chmod log file to 0600'
+                print 'unable to chmod log file to 0640'
 
     if redirect_stdout: sys.stdout = output
     if redirect_stderr: sys.stderr = output
